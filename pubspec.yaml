name: flutter_voice_recorder
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  record: ^6.0.0
  just_audio: ^0.10.4
  path_provider: ^2.1.5
  permission_handler: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
